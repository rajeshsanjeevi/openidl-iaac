pipeline {
    agent any
    tools {
        terraform 'terraform'
    }
    stages {
        stage("init") {
            steps {
                echo "terraform init started"
                terraform init
                echo "terraform init finished"
            }
        }
    }
}
